import "@Interval/styles/global.css";
import { Box } from "@Interval/components/core/box";

const LoggedOutLayout = async ({ children }: { children: React.ReactNode }) => <Box>{children}</Box>;

export default LoggedOutLayout;
